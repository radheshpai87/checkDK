"""
checkDK backend – API server only.

The CLI has moved to the cli/ folder at the root of the repository.
Run it from there inside its own virtual environment:

    cd cli
    python -m venv .venv && source .venv/bin/activate
    pip install -e .
    checkdk --help
"""





